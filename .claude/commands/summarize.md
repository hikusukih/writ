Run `.claude/scripts/summarize.sh` and present the output as-is.

If you need test suite status, also run:
- `npm test` (unit tests)
- `npm run test:integration` (integration tests)

and append the results under a `## Test Suite` section.
